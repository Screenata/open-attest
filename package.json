{
  "name": "open-attest",
  "version": "0.4.0",
  "private": true,
  "scripts": {
    "deploy": "cd server && npm install && cd admin && npm install && npm run build && cd .. && npx wrangler deploy"
  }
}
